import React, { createContext } from 'react';


export const Context = createContext();

export const Provider = () => {


    return (
        <Context.Provider 
        >
            
        
        </Context.Provider>
    );
};